# CMake build output — regenerated by `cmake -B build && cmake --build build`.
# The shared library `libyolo.{so,dylib,dll}` is consumed by
# `plugins/plugin-vision/src/yolo-detector-ggml.ts` via bun:ffi but the
# build is reproduced from source — never commit binaries.
build/
