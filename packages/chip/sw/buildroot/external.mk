# Buildroot external makefile for the Eliza e1 BSP.
#
# Package recipes are intentionally empty for now. The external tree supplies
# board metadata, a Linux config fragment, and a rootfs overlay that imports the
# e1 MMIO smoke hook.
