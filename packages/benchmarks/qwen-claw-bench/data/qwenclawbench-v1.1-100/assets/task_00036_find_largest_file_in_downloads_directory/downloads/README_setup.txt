Downloads workspace setup (benchmark)
----------------------------------------
1. Unpack any vendor bundles into a separate folder (not mixed with raw logs).
2. Point your local tool at app_config.yaml if you need defaults.
3. server_access.log is a synthetic sample for local review — do not ship externally.
4. For archival exports prefer the backup.sh helper with explicit SRC/DST.
