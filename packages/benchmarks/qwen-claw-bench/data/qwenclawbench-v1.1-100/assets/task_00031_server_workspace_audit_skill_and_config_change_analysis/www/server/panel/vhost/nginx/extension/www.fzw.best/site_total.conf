# Site total configuration for www.fzw.best
# Rate limiting
limit_req_zone $binary_remote_addr zone=fzw_limit:10m rate=30r/s;

location ~ ^/(api|admin) {
    limit_req zone=fzw_limit burst=20 nodelay;
}
