// Stub for `@elizaos/capacitor-agent` (native mobile agent bridge). UI tests
// mock Capacitor's plugin registry directly; this keeps optional imports
// resolvable in CI without loading native plugin workspace code.
export const Agent = null;

export default null;
