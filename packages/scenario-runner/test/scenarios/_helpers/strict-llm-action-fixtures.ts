/**
 * Strict fixture-authoring template — re-exported from the canonical home in
 * `@elizaos/test-harness`. Kept here as a stable import path for the existing
 * `deterministic-*.scenario.ts` files. Edit the template in
 * `packages/test/harness/action-route-fixtures.ts`.
 */
export {
  finalMessageUserText,
  matchesScenarioInput,
  type RuntimeWithScenarioLlmFixtures,
  registerStrictActionRouteFixtures,
  type StrictActionRouteFixture,
  strictActionRouteFixtures,
} from "@elizaos/test-harness/action-route-fixtures";
