/**
 * Re-export narrative feed types from the canonical source in @feed/shared.
 * Import directly from @feed/shared for new code.
 */
export type {
  ArcStateType,
  NarrativePost,
  NarrativeStory,
} from "@feed/shared";
