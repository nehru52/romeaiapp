{
  "$schema": "../profile-config.schema.json",
  "description": "Host CPU profiling matrix. Runs against the dev server when no GPU is detected. Keeps the matrix small for nightly CI and avoids MTP drafter pairings, which do not benefit CPU-only inference when the drafter shares the same kernels.",
  "models": ["eliza-1-0_8b"],
  "kvCacheConfigs": [
    { "name": "baseline-fp16", "k": "f16", "v": "f16" },
    { "name": "tbq4-tbq3", "k": "tbq4_0", "v": "tbq3_0" }
  ],
  "mtpConfigs": [{ "name": "no-mtp", "drafter": null }],
  "prompts": [
    {
      "id": "short-q",
      "text": "What is the capital of France?",
      "maxTokens": 50
    },
    {
      "id": "med-reason",
      "text": "Explain in three sentences why the sky is blue.",
      "maxTokens": 120
    }
  ],
  "iterations": 3,
  "warmupIterations": 1
}
