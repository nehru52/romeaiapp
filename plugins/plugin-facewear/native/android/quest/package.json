{
  "name": "eliza-facewear-quest",
  "version": "1.0.0",
  "description": "Meta Quest 3 Bubblewrap TWA for Eliza Facewear",
  "private": true,
  "scripts": {
    "build": "npx @bubblewrap/cli build",
    "init": "npx @bubblewrap/cli init --manifest bubblewrap.json",
    "validate": "npx @bubblewrap/cli validate"
  },
  "devDependencies": {
    "@bubblewrap/cli": "^1.21.0"
  }
}
