<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- No extra permissions required: the Bun runtime is managed by
         ElizaAgentService in the host app, and this plugin routes all
         local-agent traffic through the service request bridge. -->
</manifest>
