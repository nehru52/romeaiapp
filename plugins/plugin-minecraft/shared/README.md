# Shared Protocol Notes

This folder is reserved for shared, cross-language protocol artifacts (docs and/or generated code).

Primary source of truth:

- `plugins/plugin-minecraft/protocol/schema.json`

